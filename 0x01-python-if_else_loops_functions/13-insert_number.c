#include "lists.h"

/**
 *
 * listint_t *insert_node - inserts a node into a list 
 *
 *@head: head node of the list to be inserted
 *
 *@number: value of the list to be inserted
 *
 *Return: the address of the new node, or NULL if it failed
 *
 */

listint_t *insert_node(listint_t **head, int number)
{

	listint_t *temp;

	if (!head->next)
	{
		
	}


}
